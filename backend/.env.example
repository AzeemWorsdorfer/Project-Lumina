# Supabase credentials
SUPABASE_KEY=
SUPABASE_URL=

# OpenAI (used for reasoning + embeddings)
OPENAI_API_KEY=
OPENAI_CHAT_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Rate limiting
OPENAI_RATE_LIMIT_RPM=10

# Monthly spending cap (USD) — set hard limit in OpenAI dashboard > Billing > Usage limits
OPENAI_MONTHLY_LIMIT_USD=5
